name = "postsaja-app"
compatibility_date = "2026-06-01"
compatibility_flags = ["nodejs_compat"]

# Serve static files from dist/ and handle SPA routing
assets = { directory = "dist", not_found_handling = "single_page_application" }
